# Conditional GET/Response

The objective of this exercise is to understand how web browsers use caching by analyzing conditional HTTP GET requests. You will observe how browsers include freshness validators—such as If-Modified-Since (a timestamp) and If-None-Match with an ETag (a version identifier)—when requesting a resource that may already be cached. By comparing the first and second request/response exchanges, you will see how the server responds with 304 Not Modified (headers only, no body) when the resource has not changed, or 200 OK (headers plus body) when it has been updated. 

<details>
  <summary> Did You Know? </summary>
              Browsers use caching to save bandwidth by storing copies of web resources locally. When checking if a cached resource is still valid, they send a conditional GET request with freshness validators like If-Modified-Since (timestamp) or If-None-Match (ETag).

If the resource hasn't changed, the server responds with a 304 Not Modified status, sending only headers (no body), saving bandwidth.
If it has changed, the server responds with a 200 OK status and includes the updated resource in the body.
This mechanism allows browsers to efficiently manage cached content and reduce unnecessary data transfers. 

</details>

## Clear your browser cache

1) Close all terminal windows
2) Clear Wireshark capture
3) Clear display filter and re-enter http

 <\t>  <img width="1661" height="284" alt="image" src="https://github.com/user-attachments/assets/5bea3aa1-4750-41de-a414-53af83da481d" style="width:75%; height:auto;">

## First Request
4) Start new Wireshark capture
5) Run the following command:
   
```javascript
  curl --ipv4 -O --etag-save etag.txt http://gaia.cs.umass.edu/wireshark-labs/HTTP-wireshark-file2.html
  ```

## Second Request
5) Without stopping capture, immediately run the below command:


```javascript
curl.exe --ipv4 --etag-compare etag.txt http://gaia.cs.umass.edu/wireshark-labs/HTTP-wireshark-file2.html
```
<br />

<img width="1086" height="233" alt="image" src="https://github.com/user-attachments/assets/8ad6cfff-a809-47e5-b17f-fa626086725c" />

<br />


6) Stop Wireshark capture, save your capture.


<details>
  <summary> ** Note ** </summary>
             CURL doesn't cache by default, but we add an If-Modified-Since alternative (If-None-Match). Normally, your browser handles this for you! We use the etag flag in Windows. An ETag is a server-generated fingerprint for a file (a version ID). So if the file changes, the fingerprint changes too!

</details>


7) Answer the below questions

**First HTTP GET request of HTTP-wireshark-file2.html**
<img width="1661" height="800" alt="image" src="https://github.com/user-attachments/assets/37ac449e-fab6-4aaf-a0a4-7cb2f5fa10c9" />

   * Inspect the contents of the first HTTP GET request of HTTP-wireshark-file2.html from your host to the server. Do you see an "If-None-Match" line in the HTTP GET? **ANSWER** No


**First server response**

<img width="1662" height="839" alt="image" src="https://github.com/user-attachments/assets/af6201d0-0ebc-4946-8ebe-eedccfacd898" />

  * Inspect the contents of the server response.  Did the server explicitly return the contents of the file? **ANSWER:** Yes


**Second HTTP GET request**

  <img width="1661" height="792" alt="image" src="https://github.com/user-attachments/assets/931c80b8-f209-424a-add5-e78db61dd072" />

  * Now inspect the contents of the second HTTP GET request for HTTP-wireshark-file2.html from your host to the server.  Do you see an "If-None-Match" line in the HTTP GET? **ANSWER:** Yes

  * If so, what information follows the “IF-MODIFIED-SINCE:” header? **ANSWER:** "173-64231b67176b7"

**First server response**

<img width="1661" height="796" alt="image" src="https://github.com/user-attachments/assets/8bbf779e-272e-4697-aaa7-a69de048c216" />


  * What is the HTTP status code and phrase returned from the server in response to this second HTTP GET? **ANSWER:** 304
  * Did the server explicitly return the contents of the file? **ANSWER:** No

